#version 330 core

in vec3 fragment_color;
in vec2 fragment_texture_coord;

out vec4 screen_color;

uniform sampler2D my_texture;

void main(){
    screen_color = vec4(fragment_color, 1.0) * (texture(my_texture, fragment_texture_coord));
} 